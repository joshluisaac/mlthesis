package com.joshluisaac.mlthesis.utils;

/**
 * LibSVM allows for sparse training data. That is, the non-zero values are the
 * only ones that are included in the dataset. Hence, the index specifies the
 * column of the instance data (feature index). To convert from a conventional
 * dataset just iterate over the data, and if the value of X(i,j) is non-zero,
 * print j+1 : X(i,j).
 **/

public class Csv2Libsvm {

  public void transformSparseToDense() {

  }

}
